
/** 
 * AI service deactivated to optimize performance and speed.
 */
import { DataRow } from "../types";

export const getAIInsight = async (match: DataRow): Promise<string> => {
  return ""; // AI context removal per request
};
